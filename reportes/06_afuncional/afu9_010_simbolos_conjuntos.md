```{=latex}
\clearpage
```

```{=latex}
\LaTeX
```

# Tablas de símbolos {.unnumbered}


| Significado               | Símbolo  |
|:------------------------- |:------------------------------------|
| Pertenece a               | $\in$ `\in`                          |
| No pertenece a            | $\notin$ `\notin`                    |
| Contiene como elemento    | $\ni$ `\ni`                          |
| Subconjunto               | $\subset$ `\subset`  |
| Subconjunto o igual       | $\subseteq$ `\subseteq`                 |
| Subconjunto propio        | $\subsetneq$ `\subsetneq`    |
| No es subconjunto         | $\nsubseteq$ `\nsubseteq`    |
| Superconjunto             | $\supset$ `\supset`       |
| Superconjunto o igual     | $\supseteq$ `\supseteq`     |
| Superconjunto propio      | $\supsetneq$ `\supsetneq`    |
| Unión                     | $\cup$ `\cup`          |
| Unión grande              | $\bigcup$ `\bigcup`       |
| Intersección              | $\cap$ `\cap`          |
| Intersección grande       | $\bigcap$ `\bigcap`       |
| Diferencia                | $\setminus$ `\setminus`     |
| Complemento               | $A^c$           |
| Complemento (barra)       | $\overline{A}$ `\overline{A}`  |
| Conjunto vacío            | $\emptyset$ `\emptyset`      |
| Producto cartesiano       | $\times$ `\times`         |
| Potencia de un conjunto   | $\mathcal{P}(A)$ `\mathcal{P}(A)` |
| Existe                    | $\exists$ `\exists`        |
| Existe único              | $\exists!$ `\exists!`       |
| Para todo                 | $\forall$ `\forall`        |
| Tal que                   | $\mid$ `\mid`           |
| Igual por definición      | `:=` |

: Notación en LaTeX para conjuntos

\newpage

| Símbolo               | Significado                        | LaTeX                 |
| --------------------- | ---------------------------------- | --------------------- |
| $\neg P$              | Negación                           | `\neg P`              |
| $P \land Q$           | Conjunción (y)                     | `P \land Q`           |
| $P \lor Q$            | Disyunción (o)                     | `P \lor Q`            |
| $P \oplus Q$          | Disyunción exclusiva (xor)         | `P \oplus Q`          |
| $P \rightarrow Q$     | Implicación                        | `P \rightarrow Q`     |
| $P \leftarrow Q$      | Implicación inversa                | `P \leftarrow Q`      |
| $P \leftrightarrow Q$ | Equivalencia lógica                | `P \leftrightarrow Q` |
| $P \Leftrightarrow Q$ | Equivalencia                       | `P \Leftrightarrow Q` |
| $P \Rightarrow Q$     | Implica (meta-lógico)              | `P \Rightarrow Q`     |
| $P \Leftarrow Q$      | Implicación inversa                | `P \Leftarrow Q`      |
| $\therefore$          | Por lo tanto                       | `\therefore`          |
| $\because$            | Porque                             | `\because`            |
| $\vdash$              | Se demuestra / deriva              | `\vdash`              |
| $\models$             | Consecuencia lógica                | `\models`             |
| $\vDash$              | Satisface / consecuencia semántica | `\vDash`              |
| $\bot$                | Falso / contradicción              | `\bot`                |
| $\top$                | Verdadero                          | `\top`                |

: Lógica proposicional


| Símbolo      | Significado   | LaTeX        |
| ------------ | ------------- | ------------ |
| $\forall x$  | Para todo (x) | `\forall x`  |
| $\exists x$  | Existe (x)    | `\exists x`  |
| $\nexists x$ | No existe (x) | `\nexists x` |

: Cuantificadores (Lógica de primer orden)

\newpage	